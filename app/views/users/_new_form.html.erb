<%= form_for @user do |f| %>
	<% if user.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
			<ul>
				<% user.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<%= f.label 'Username' %><br>
	<%= f.text_field :username %>
	<%= f.hidden_field :type, :value => 'Neutrality' %>
	<%= f.submit %>
<% end %>